# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

language: r

r:
  - release
  - devel

cache: packages

r_packages:
  - covr

addons:
  apt:
    update: true
    packages:
      - libsodium-dev
      - libprotobuf-dev
      - protobuf-compiler

